import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"], variable: "--font-inter" });

export const metadata: Metadata = {
  title: "Meritos | Find vetted researchers & talent for your startup",
  description: "Meritos connects founders with pre-vetted researchers, students, and innovators who solve real problems. Post your first challenge free.",
  keywords: "startup talent, researcher for hire, innovation challenges, founder tools",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className={`${inter.className} antialiased`}>
        {children}
      </body>
    </html>
  )
}